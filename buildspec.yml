version: 0.1

phases:
  install:
    commands:
      - mkdir apis/admin-panel
      - cd apis/admin-panel && npm i
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
      - apis/admin-panel/npm-debug.log
artifacts:
  files:
    

